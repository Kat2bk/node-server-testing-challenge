const request = require('supertest');
const server = require('../../api/server');

describe('parakeet router', function() {
    if ('able to run tests?', () => {
        expect(true).toBe(true);
    });
})

describe('GET /api/parakeets')